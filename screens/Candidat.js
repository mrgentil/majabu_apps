import React from "react";
import {
    SafeAreaView,
    View,
    Text,
    StyleSheet,
    TouchableOpacity,
    Image,
    FlatList
} from "react-native";

import { icons, images, SIZES, COLORS, FONTS } from '../constants'

const Candidat = () => {
    return(
        <View>
            <Text>Les Candidats</Text>
        </View>
    )
}

export default Candidat;